# Minimal Synthetic Live Verification of Issue-ID Local Binding v1

Parent: `137ba156e72495a2ea9521178c4247b16d969d75`

Status: **blocked before transport**. Provider calls: 0.

The exact previously failed Issue-ID Constraint Isolation request was reconstructed at 9,517 request bytes and 5,394 provider-schema bytes. Removing only `$.response_format.json_schema.schema.properties.issueId.pattern` produces a deterministic 9,469-byte request with a 5,346-byte schema and SHA-256 `52bab898d27bf848cc11a754fefcae764f7964ebf5f004b70e57fd72606d3032`. Unexpected changed paths are zero.

That exact one-change request does not match the separately required accepted schema identity of 5,424 bytes and SHA-256 `74144ebf131667591b8b9e1897f6a6585717847c04e0499f1989f2f100dfe08c`. It also predates the Package B request-scoped reference map: it contains no provider aliases and cannot prove alias membership or reverse mapping if output is returned.

The current synthetic request using the accepted Package B/C builder does match the required 5,424-byte schema identity, but compared with the previously failed isolation request it also changes the schema name, removes the packet-specific inputHash const, and expands the evidence-class enum. Those are unexpected material changed paths under this authorization.

Because neither candidate satisfies every mandatory pre-call condition, no request was sent. HTTP status, structured output, tokens, cost, and latency are unavailable. No protected canonical/commercial state or source file changed.

Product must choose a test anchor before any live call: either the exact 5,346-byte one-change historical request without Package B validation, or the current 5,424-byte Package B/C synthetic request with the additional differences explicitly authorized. Compatibility remains untested and no production-readiness claim is made.
