# Statement Structure and Printed Membership v1

Evaluation-only vertical slice, uncommitted. Scope: native-text fee tables and funding/batch tables, with adjacent summaries as negative boundaries. No financial population identity, canonical claims, AI, OCR, runtime integration, extractor replacement, benchmark changes or authority changes.

Engine consumes immutable, source-hashed positioned native fragments. It constructs geometric line/row proposals, source-bound header paths and column assignments; records row/table and printed total membership; allows only evidence-supported page links. Raw text is preserved, whitespace normalization separately recorded; no digit/sign repair, amount-target matching, subset-sum member selection, merchant/fixture-specific branching, file-name dispatch or rescue-topic imports.

Known development cases reuse six previously studied native PDFs and existing visual/source annotations. Review outputs against complete relevant original pages, including unselected rows. Test ambiguity by removing headers, fragments and continuation pages, duplicating rows and creating incompatible adjacent tables. Require useful positive recovery as well as explicit unresolved/rejected cases. Printed scope is distinct from financial membership and arithmetic correctness. Schema inference and association are proposals with explicit admission rules, not extractor truth.

Untouched holdout: existing fixtures and public teaching samples are excluded. Request user-provided genuinely untouched native statements and/or acquire new authentic publicly posted native statements; hash-deduplicate against known corpus. Do not inspect their contents until implementation and acceptance protocol are frozen. Record acquisition, dates, scope, code hash and any inability to obtain fresh data. Any fix after unblinding must invalidate holdout status; do not quietly retune.

Completion gate: source-bound accepted rows/columns and clear printed member maps on known positives; no accepted source mutation or ambiguous membership in adversarial cases; useful accepted coverage plus manually reviewed precision and explicit failures on untouched holdouts. Preserve known source anomalies. Canonical path and all existing files unchanged; disconnected implementation has no runtime import path and grants no authority. No claim that all Fiserv layouts are supported or any financial truth has expanded.

## Frozen acceptance review before holdout unblinding

Known anchor subset follows scope, not performance: P2, F3, Z1, Z2, Z3, B3, O1, O2, O3, N1, N2, N3 from the previous source gold (12 rows). Card/day/submitted schemas remain outside this vertical slice; their raw positioned fragments and clustered unassigned lines must survive. Review complete selected fee tables, the Priority funding continuation and BASYS batch continuation against rendered source, not just matched totals. Headerless fee continuation is an explicit unresolved capability gap; no inference of complete scope from matching sums.

Holdout selection: newly obtained Monroe Clerk public procurement exhibit, SHA256 in sealed manifest. Select the first three complete contiguous native-text card-processing statements in physical-page order, excluding the exact account exposed by the search preview and any source/statement duplicate already used. Selection uses printed pagination and document type only, never extraction success. At least 500 native characters per statement page; no OCR. Process statements independently with original physical-page/byte provenance, never combine their financial content. If fewer eligible statements exist, disclose this rather than substitute reused examples.

After code freeze, annotate each selected statement's in-scope table regions, complete printed member counts for every reviewed total, and sample the first/middle/last charged rows before opening structure outputs. Report detected/missed regions, accepted/unresolved/wrong rows and totals separately; require meaningful positive recovery and zero observed incorrect accepted memberships before recommending advancement. Zero observed errors is not a universal accuracy claim. No engine fixes or parameter changes after holdout inspection; a discovered defect is a failure to report and needs a new holdout for subsequent validation.
