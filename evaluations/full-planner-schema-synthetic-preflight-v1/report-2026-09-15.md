# Full Planner Schema Synthetic Preflight v1

Status: **FAILED — TIMEOUT**
Recommendation: **TRANSPORT_FAILURE**

- Provider: OpenRouter
- Requested model: `anthropic/claude-opus-4.6`
- Provider calls: 1/1
- Retries/fallbacks: 0/0
- Failure phase: response-body read
- Timeout boundary: 20,000 ms
- Command wall time: 21,029 ms
- Returned model/provider: not recovered
- Tokens/cost: unavailable; possible billable usage remains unknown

The request used the complete synthetic planner structure. The local schema was 6,861 bytes and the translated provider schema was 6,011 bytes. Translation removed 18 `minLength`, 18 `maxLength`, and 22 `maxItems` constraints. The provider schema retained one supported nullable `anyOf`, one simple hash pattern, 21 `minItems` constraints, and no refs, recursion, `oneOf`, or `allOf`.

The synthetic packet was 2,350 bytes and passed privacy/boundary checks. It contained no merchant identity, Gold data, real fee label, real amount, real processor, evidence reference, account data, filename, PDF, raw statement text, or customer data.

No structured response completed, so provider-schema validation, strict local planner validation, reference grounding, and qualitative review could not be performed. No retry, fallback, Gold evaluation, research, source admission, truth mutation, customer output, or production routing occurred.

The abort escaped while reading the response body, revealing an evaluation telemetry gap. That path is now classified safely for future evaluations, but Product authorized no second call and none was made.
