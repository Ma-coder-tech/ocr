# Issue-ID Local-Binding Compatibility Fix v1

Parent: `ebd928ef506c396485d75ba639d4da367340d504`

The provider-facing `issueId` schema is now structurally `{ "type": "string" }`. No packet-specific const or provider-side issue-ID pattern remains. Exact issue identity remains trusted local state: provider output must equal the originating request's issue ID before reference restoration and planner admission.

Local validation accepts the exact issued ID and fails closed for a one-character difference, another request's ID, an invented ID, a missing/non-string ID, valid aliases paired with the wrong ID, conflicting identity-key spellings, and duplicated top-level JSON keys. Input-hash binding and all Package B alias checks remain unchanged.

The provider schema is stable at 5,424 bytes with SHA-256 `74144ebf131667591b8b9e1897f6a6585717847c04e0499f1989f2f100dfe08c`. It is identical across all six historical requests, all 60 issues from 11 supported-Fiserv Gold statements, and stress fixtures with 0 through 512 references.

Gold validation produced 60/60 exact reference round trips and 60/60 accepted planner validations. Every Package B privacy/alias acceptance counter is zero. Canonical, RD, commercial, activity, qualification, sensitivity, participant/control, completeness, and materiality invariants are unchanged 11/11. The governed commercial-source SHA remains `a204de0fa0bf4cedfb852ff32327b22f43d5b38c7f6eeb8bea4a26bf417bf456`.

Focused planner tests improved from 54/54 at the parent to 56/56 with the new fail-closed cases. TypeScript build passed. The exhaustive comparison found zero new failing tests or files: all 83 implementation failures reproduce parent signatures. One unrelated production-worker failure from the parent did not reproduce on the implementation and is recorded as baseline resource/state-sensitive; no implementation fix is claimed.

Provider calls: 0. Network calls: 0. No research, evidence admission, customer output, production routing, or truth mutation occurred.

There is no architecture conflict. The offline evidence supports Product review of this fix and, only after separate authorization, one minimal synthetic live verification. It does not prove live provider compatibility.
